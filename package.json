{
  "name": "pmlio-helper",
  "version": "1.0.2",
  "description": "PMLio Desktop Signing Helper — localhost bridge for USB token signing",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development electron .",
    "test": "jest --verbose",
    "test:integration": "jest --testPathPattern=integration --verbose",
    "build:win": "electron-builder --win",
    "build:mac": "electron-builder --mac",
    "build": "electron-builder --win --mac"
  },
  "author": "PMLio s.r.o.",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "node-forge": "^1.3.1",
    "selfsigned": "^2.4.1",
    "uuid": "^10.0.0"
  },
  "optionalDependencies": {
    "pkcs11js": "^2.1.6"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-builder": "^24.13.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
